# 주제 발굴·구조 다양화 작업 계획서

> 작성 2026-09-13
> 선행 검토: `claudedocs/agentic_generation_review_20260913.md` · `naver_extension_review_20260913.md` · `benchmark_plan_20260913.md`

## 0. 진단 — 재조합이 왜 최신성을 못 만들었나

먼저 지적하신 재조합 문제부터 답합니다. **프롬프트 튜닝 문제가 아니라 입력 부재 문제입니다.**

`generation/title_recombiner.py:26`

```python
DEFAULT_TITLE_PROMPT = (
    "다음 블로그 제목을 SEO에 최적화된 형태로 재조합해주세요.\n"
    "원본 제목: {title}\n\n"
    "규칙:\n"
    "- 원본의 핵심 키워드를 유지\n"
    ...
)
```

**입력이 원본 제목 문자열 하나뿐입니다.** 트렌드 신호도, 최신 자료도, 사람들이 실제로 뭘 묻는지도 들어가지 않습니다. **모델에게 주어진 정보가 원본 제목뿐인데 최신성 있는 제목이 나올 수는 없습니다.** 표현만 바꿀 수 있습니다.

코드 주석이 이미 이 한계를 두 번 인정합니다.

> `# 핵심어를 명시한다. 이게 없으면 재조합이 검색되는 말을 흘린다.`
> `# 실제로 다섯 스타일이 거의 같은 제목으로 수렴했다.`

**재조합은 "변형기"입니다.** 있는 제목의 표현을 바꿉니다. 기대하신 건 "발굴기"였고, 그건 다른 물건입니다.

→ **결론: 재조합을 고치는 게 아니라, 재조합을 거치지 않는 새 경로를 만들어야 합니다.**

---

## 1. 질문 네 건에 대한 답

### 1-1. 홈피드 트렌드 분석과 지식인 주제 추출을 워크플로우로 옮길 수 있는가

**가능합니다. 그리고 생각보다 훨씬 쉽습니다.**

지금 쓰는 네이버 검색 API는 **엔드포인트만 바꾸면 다른 소스가 열립니다. 인증 키는 그대로입니다.**

```
현재: https://openapi.naver.com/v1/search/webkr.json   ← 웹문서 하나
```

| 엔드포인트 | 용도 |
|---|---|
| `kin.json` | **지식iN** — 수작업으로 하신 그 소스 |
| `cafearticle.json` | 카페 — 실사용 후기·질문 |
| `news.json` | 뉴스 — 최신성 검증용 |
| `encyc.json` | 백과 — 용어·정의 |
| `doc.json` | 전문자료 |

`sort=date` 를 주면 최신순 정렬도 됩니다.

**즉 `kin.json` 하나 붙이면 지식인 워크플로우의 입력이 확보됩니다.** 별도 크롤링도, 화면 제어도 필요 없습니다. `reference_search_service.py`(147줄)에 소스 어댑터를 추가하는 수준입니다.

홈피드 트렌드는 API가 없어 다릅니다. 인기 검색어는 **수집 대상**이지 검색 API가 아니라, 별도 수집 경로가 필요합니다(난이도 중간). 다만 **트렌드는 지식인보다 우선순위가 낮습니다.** 뒤에서 설명합니다.

### 1-2. 인기 검색어를 넣으면 결과가 달라지는가

**재조합을 그대로 두면 별로 안 달라집니다.**

인기 검색어를 어디에 넣느냐가 전부입니다.

| 투입 지점 | 결과 |
|---|---|
| 재조합 단계 | 원본 제목이 여전히 주인공. **키워드 하나 갈아끼운 수준** |
| **주제 발굴 단계** | 주제 자체가 달라짐 |

지금 구조에서 인기 검색어를 재조합에 넣으면 `"지커 코리아 7X"` 같은 단어가 기존 제목에 끼워 넣어질 뿐입니다. **주제는 그대로입니다.**

그래서 제안을 수정합니다. **인기 검색어는 재조합이 아니라 주제 발굴 단계에 들어갑니다.** 그리고 **새 경로는 재조합을 거치지 않습니다.** 재조합은 기존 수집 제목 경로에만 남겨둡니다.

### 1-3. 태그는 애드센스에 효용이 있는가 — 제안을 철회합니다

**지적이 맞습니다. A-3을 기각합니다.**

| 플랫폼 | 태그 효용 |
|---|---|
| 네이버 블로그 | **있음.** 검색 노출에 실제 기여 |
| 워드프레스 | **낮음, 오히려 위험** |
| Blogger | **낮음** |

**워드프레스**: 태그마다 아카이브 페이지가 자동 생성됩니다. 글 하나에 태그 다섯 개면 **본문이 거의 없는 목록 페이지 다섯 장**이 색인 대상이 됩니다. 자동 발행이면 이게 누적돼 **얕은 페이지가 본문 수의 몇 배로 불어납니다.** SEO 플러그인들이 태그 아카이브를 기본 `noindex`로 두는 이유입니다.

**Blogger**: 라벨 페이지도 같고, `?max-results` 같은 파라미터로 중복 URL까지 생깁니다.

**애드센스 관점에서 특히 불리합니다.** 이미 `guntamoney`가 "가치 없는 콘텐츠"로 반려된 이력이 있습니다. 얕은 페이지 양산은 정확히 그 심사에 걸리는 유형입니다.

→ **태그는 네이버를 실제로 하게 될 때만 다시 꺼냅니다.**

### 1-4. A-1이 잘 되면 재검색이 필요 없지 않은가

**정확한 지적이고, 제 설명이 부족했습니다. 순서를 뒤집어 설명하겠습니다.**

재검색은 **목적이 아니라 결과**입니다. 제가 수작업에서 실제로 한 순서는 이렇습니다.

```
1. 질문을 읽고 "이 사람이 진짜 막힌 지점"을 찾는다
2. 그걸 답하려면 무엇을 알아야 하는지 [목록]을 만든다   ← 검색 전
3. 목록의 각 항목을 별도 쿼리로 던진다 (병렬)
4. 결과를 [목록]에 대조한다 — 빈칸이 있나
5. 빈칸이 있으면 그 항목만 다시 검색
6. 채워진 목록으로 구조를 짜고 쓴다
```

**핵심은 2번입니다.** 퇴거 청소 글에서는 이 목록이었습니다.

- 원상회복 의무의 법적 근거는 무엇인가
- 통상손모는 누구 책임인가
- 계약서 특약이 있으면 달라지는가
- 보증금에서 임의 공제됐을 때 밟을 절차는

**이 목록이 있으니 "자료가 충분한가"를 판정할 수 있었습니다.** 목록이 없으면 판정 기준 자체가 없습니다.

말씀하신 **"보이지 않는 부분"이 이 목록입니다.** 블로그 오토에는 이게 없습니다. 제목 하나에서 쿼리 하나를 뽑고 끝이라, 무엇이 채워졌고 무엇이 비었는지 알 방법이 없습니다.

**그래서 답은 이렇습니다.** A-1로 소스를 늘리면 자료의 양과 질이 올라가 재검색 **빈도는 줄어듭니다.** 하지만 **목록 없이는 애초에 재검색을 해야 할지조차 판단할 수 없습니다.** 재검색 루프를 만드는 게 목표가 아니라, **근거 목록을 만드는 게 목표**이고 재검색은 거기 딸려오는 부속입니다.

이 문서에서는 이 목록을 **근거 체크리스트**라고 부르겠습니다.

---

## 2. 주제 발굴 워크플로우 재설계

### 2-1. 지식인을 왜 쓰는지 — 로직으로 옮기기

말씀하신 통찰이 설계의 출발점입니다.

> "해당 니치를 확장하기 위한 도구로 지식인을 삼은 것입니다. 집단지성의 힘을 빌린 것이죠. 많은 사람들이 각각의 상황에 따라 같은 주제라도 다른 궁금증이 생길 것이고 그것이 글의 주제가 될 수 있다."

실제 결과가 이를 증명합니다. **「이사 견적」이라는 니치 하나에서 11편이 나왔습니다.**

| 글 | 상황 축 |
|---|---|
| 송림동→주안동 | 지역 간 근거리 |
| 15평→32평 | 평수 확대 |
| 사다리차 vs 포장이사 | 운반 방식 |
| 서울→천안 | 장거리 |
| 보관이사 | 일정 공백 |
| 파주→부산 소형 | 장거리 + 소량 |
| 사다리차 비용 | 단가 |
| 원룸 견적 | 최소 규모 |
| 같은 건물 2→3층 | 거리 0 |
| 경유 이사 | 합가 |
| 일반 vs 반포장 | 서비스 범위 |

**키워드로는 이게 나오지 않습니다.** 「이사 견적」에서 "같은 건물 2층에서 3층"은 도출되지 않습니다. **상황은 사람이 만들어냅니다.**

로직으로 옮기면 한 줄입니다.

```
주제 = 니치 × 실제 사람의 구체적 상황
```

니치는 우리가 갖고 있고, **상황은 지식인·카페에 있습니다.**

### 2-2. 새 경로 설계

```
① 니치 (topic/subtopic)
     ↓
② 질문 수집        kin.json + cafearticle.json (sort=date)
     ↓             니치 키워드로 실제 질문을 긁는다
③ 상황 추출        질문에서 [상황 축]을 뽑는다
     ↓             "2층→3층 / 엘베없음 / 장롱3짝" 같은 조건
④ 군집·중복 제거   같은 상황끼리 묶고, 이미 쓴 주제 제외
     ↓             (기존 topic_dedup_gate 재사용)
⑤ 주제 후보 산출   상황 + 니치 → 주제 문장
     ↓             ※ 재조합 안 거침
⑥ 트렌드 가중      인기검색어·뉴스와 겹치면 우선순위 ↑
     ↓
⑦ 최신성 판정      제도 변경·시의성 확인 (news.json)
     ↓
⑧ 제목 생성        상황을 제목에 녹인다
     ↓
⑨ 근거 체크리스트   "이 제목에 답하려면 무엇을 알아야 하나" ★
     ↓
⑩ 항목별 병렬 검색  체크리스트 항목 = 쿼리 1개. 소스는 항목 성격에 따라
     ↓
⑪ 충족 판정        빈칸 확인 → 빈칸만 재검색 (1회 상한)
     ↓
⑫ 구조 설계        체크리스트 순서로 소제목 구성
     ↓
⑬ 본문 생성
     ↓
⑭ 자체 검증        상투어·금지태그·1인칭 빈도
```

**⑨가 이 설계의 중심입니다.** ⑩·⑪·⑫가 전부 ⑨에 의존합니다.

### 2-3. 기존 경로와의 관계

**기존 경로를 없애지 않습니다. 나란히 둡니다.**

| 경로 | 출처 코드 | 재조합 | 용도 |
|---|---|---|---|
| 키워드 → 제목 수집 | `title_collect` | **거침** | 기존 유지 |
| 도메인 일괄 수집 | `domain_extract` | **거침** | 기존 유지 |
| 키워드 AI 생성 | `keyword_module` | 안 거침 | 기존 유지 |
| 뉴스 결합 | `news_gen` | 안 거침 | 기존 유지 |
| **질문 발굴** | `SRC_QUESTION` **신설** | **안 거침** | 니치 확장 |

`title_source.py`에 코드 하나를 추가하는 형태라 기존 화면·필터·통계가 그대로 붙습니다. **출처별 승격률 비교도 자동으로 됩니다.**

### 2-4. 근거 체크리스트 상세

**생성 시점**: 제목 확정 직후, 검색 전

**산출 형태**

```json
{
  "title": "원룸 퇴거 청소비, 내야 할 때와 안 내도 될 때",
  "core_question": "퇴거 청소는 법적 의무인가",
  "checklist": [
    {"id": 1, "ask": "원상회복 의무의 법적 근거", "source": "law", "filled": false},
    {"id": 2, "ask": "통상손모의 책임 주체", "source": "law", "filled": false},
    {"id": 3, "ask": "청소비 특약의 효력 요건", "source": "web|kin", "filled": false},
    {"id": 4, "ask": "보증금 임의공제 대응 절차", "source": "web|news", "filled": false}
  ]
}
```

**`source` 필드가 소스 라우팅을 합니다.** 법령 질문은 법령 소스로, 경험 질문은 지식인·카페로, 시의성 질문은 뉴스로 갑니다. **"검색 기준이 더 디테일하다"고 하신 부분이 이것입니다.**

**충족 판정**: 항목별로 `filled` 를 채웁니다. 미충족 항목만 쿼리를 바꿔 1회 재검색하고, 그래도 안 채워지면 **그 항목을 빼고 진행**합니다(글을 막지 않음).

**구조 설계**: 채워진 항목이 소제목 순서가 됩니다. 항목이 다르면 구조가 다릅니다. 퇴거 청소와 경유 이사의 구조가 달랐던 이유가 여기입니다.

### 2-5. 트렌드의 위치 — 우선순위를 낮춥니다

인기 검색어는 **⑥ 가중치**로만 씁니다. 주제를 만들지 않습니다.

이유가 둘입니다. 첫째, 인기 검색어는 **상황이 없습니다.** `"지커 코리아 7X"` 만으로는 글의 축이 안 나옵니다. 둘째, **경쟁이 가장 심한 구간**이라 신생 블로그가 먹기 어렵습니다.

**지식인 질문은 반대입니다.** 상황이 구체적이고 경쟁이 적습니다. **먼저 할 것은 지식인이고, 트렌드는 나중입니다.**

---

## 3. 구조 다양화 — 제안하신 4건 검토

네 건 모두 **타당하고 반영합니다.** 코드와 대조한 결과를 붙입니다.

### 3-1. 니치별 프롬프트 모듈 분리 — **데이터 구조는 이미 준비돼 있습니다**

`generation/adsense_niche.py:15`

```python
def resolve_module_niche(module_settings) -> Optional[List[int]]:
    ...
    ids = [int(x) for x in (module_settings.get("niche_topic_ids") or []) ...]
```

**`niche_topic_ids`가 이미 리스트입니다.** 복수 니치를 담을 수 있습니다.

`FlowModule`도 `execution_order`를 갖고 있어 **한 플로우에 모듈을 여럿 매달 수 있습니다.**

**필요한 건 둘뿐입니다.**
1. UI — 블로그 선택 시 니치가 전부 선택되는 동작을 **부분 선택 가능**하게
2. 실행 — 생성 시 제목의 topic_id를 보고 **담당 모듈을 고르는 분기**

난이도: 중간. 마이그레이션 불필요.

### 3-2. 동일 니치 복수 프롬프트 로테이션 — **이게 제 「지면별 프리셋」보다 낫습니다**

지적하신 근거가 정확합니다.

> "한 블로그에서 동일한 구조로 글이 발행되면 검색사이트에서 걸러낼 수 있다고 보았습니다."

제가 앞 보고서에서 제안한 「지면별 프리셋」은 **축이 하나**입니다. 지면이 정해지면 구조도 하나로 고정됩니다. 로테이션은 **같은 축 안에서도 매번 달라집니다.** 이쪽이 맞습니다.

**설계**

```json
"prompt_rotation": {
  "enabled": true,
  "mode": "random | sequential | by_keyword | by_niche",
  "prompt_ids": [12, 15, 19],
  "cursor": 0
}
```

| 모드 | 동작 |
|---|---|
| `random` | 매 글 무작위 |
| `sequential` | 순번대로 (모듈별 커서 저장) |
| `by_keyword` | 키워드 패턴 → 프롬프트 매핑 |
| `by_niche` | 하위 니치 → 프롬프트 매핑 |

**`sequential`은 커서 저장이 필요합니다.** 모듈 settings에 두면 동시 실행 시 경합이 생기니, 기존 `flow_execution_state` 계열에 붙이는 편이 안전합니다.

난이도: 중간.

### 3-3. 애드센스 / CPA 분기 — **기존 스위치를 확장하면 됩니다**

`generation/adsense_prompt_switch.py`에 승인용 프리셋 전환 로직이 이미 있습니다(`APPROVAL_PRESET_KEY`, `find_preset()`). **같은 자리에 목적 축을 하나 더 넣는 형태**입니다.

```
목적: adsense | cpa | info
  ↓
목적별 프롬프트 집합 → 그 안에서 3-2 로테이션
```

말씀대로 **애드센스용과 CPA용은 구조가 다를 수밖에 없습니다.** 수작업 CPA 글은 `쟁점 → 근거 → 판단 기준 → 실행 조언 → CTA` 였고, 정보성 글(장기수선충당금·첫 독립)은 CTA가 아예 없었습니다.

난이도: 낮음.

### 3-4. 템플릿 이미지 복수 — **현재 단수 하드코딩입니다**

`generation/template_image_service.py:221`

```python
raw_path = config.get("template_image")
```

**문자열 하나입니다.** 복수화하려면 이렇게 갑니다.

```json
"template_images": [
  {"path": "blogs/21/template/t1.png", "topic_ids": [24]},
  {"path": "blogs/21/template/t2.png", "topic_ids": [24]},
  {"path": "blogs/21/template/t3.png", "topic_ids": []}
],
"template_image_mode": "random | by_niche | by_keyword | sequential"
```

**하위 호환이 필수입니다.** 블로그 21이 이미 `template_image` 단수로 운영 중이라, `template_images`가 없으면 기존 키를 읽도록 폴백을 둡니다.

난이도: 낮음. 오버레이 설정은 공유하고 배경만 바뀌므로 `overlay_config`의 나머지 필드는 손대지 않습니다.

---

## 4. 작업 단계

의존 관계를 반영해 넷으로 나눴습니다.

### 1단계 — 소스 확장 (선행 필수)

| 항목 | 파일 | 난이도 |
|---|---|---|
| 네이버 검색 소스 어댑터화 | `reference_search_service.py` | 낮음 |
| `kin` · `cafearticle` · `news` 추가 | 〃 | 낮음 |
| 소스별 라우팅 인터페이스 | 신규 모듈 | 낮음 |

**여기가 막히면 2·3단계가 전부 막힙니다.** 먼저 합니다.

### 2단계 — 질문 발굴 경로

| 항목 | 파일 | 난이도 |
|---|---|---|
| 출처 코드 `SRC_QUESTION` 추가 | `title_source.py` | 낮음 |
| 질문 수집·상황 추출 | 신규 서비스 | 중간 |
| 군집·중복 제거 | 기존 `topic_dedup_gate` 재사용 | 낮음 |
| 주제 후보 → 제목 생성 | 신규 (재조합 우회) | 중간 |

### 3단계 — 근거 체크리스트 ★

| 항목 | 파일 | 난이도 |
|---|---|---|
| 체크리스트 생성 | 신규 모듈 | 중간 |
| 항목별 병렬 검색 | `reference_collector.py` 수정 | 중간 |
| 충족 판정 + 선별 재검색 | **신규 모듈로 분리** | 중간 |
| 구조 설계 (소제목 구성) | 신규 | 중간 |
| 자체 검증 규칙 | `quality_gate.py` | 낮음 |

> `reference_collector.py`가 이미 465줄입니다. **500줄 제한에 걸리므로 판정 로직은 반드시 별도 모듈로 뺍니다.**

### 4단계 — 구조 다양화

| 항목 | 파일 | 난이도 |
|---|---|---|
| 템플릿 이미지 복수 | `template_image_service.py` | 낮음 |
| 애드센스/CPA 목적 분기 | `adsense_prompt_switch.py` | 낮음 |
| 프롬프트 로테이션 | 모듈 settings + 선택 로직 | 중간 |
| 니치별 모듈 분리 UI | 프론트 + 실행 분기 | 중간 |

**4단계는 1~3단계와 독립적입니다.** 원하시면 먼저 해도 됩니다. 특히 템플릿 이미지 복수화와 목적 분기는 난이도가 낮아 빠르게 체감됩니다.

---

## 5. 전제와 리스크

**호출 비용.** 체크리스트를 넣으면 글 1편당 AI 호출이 **3회 → 6~8회**가 됩니다. 재검색 상한(1회)과 체크리스트 항목 상한(4~5개)을 **코드에 고정**해야 합니다.

**모델 배분은 실측이 필요합니다.** 체크리스트 생성과 충족 판정은 **판단 작업**이라 가성비 모델이 제대로 못 할 수 있습니다. 반대로 본문 생성은 가성비 모델로도 됩니다. **단계별로 모델을 나누는 편이 유리할 가능성이 크지만, 같은 제목으로 A/B를 돌려 확인해야 합니다.** 넣고 나서 품질이 그대로면 호출만 두 배가 됩니다.

**지식인 수집량.** 네이버 검색 API는 일일 호출 한도가 있습니다. 질문 수집을 매 글마다 하지 말고 **니치별로 주기 수집해 풀에 쌓아두고 꺼내 쓰는 구조**가 맞습니다.

**저작권.** 지식인 질문을 **그대로 인용하지 않습니다.** 상황 축만 뽑아 쓰고 각색합니다. 이번 수작업에서 지킨 방식 그대로입니다.

**중복.** 같은 니치에서 질문을 계속 뽑으면 상황이 겹칩니다. ④ 단계의 중복 제거가 실효를 내야 하고, **이미 발행한 글의 상황 축을 저장해 대조**해야 합니다.

---

## 6. 변경 사항 요약

앞 보고서 대비 바뀐 것입니다.

| 항목 | 이전 | 이번 |
|---|---|---|
| A-3 태그 | 채택 | **기각** (애드센스에 불리, 네이버 도입 시 재검토) |
| A-2 인기 검색어 | 주제 소스 | **가중치로 강등** (상황이 없어 주제가 안 됨) |
| B-1 재검색 루프 | 독립 항목 | **체크리스트의 부속으로 재정의** |
| C-1 지면별 프리셋 | 채택 | **프롬프트 로테이션으로 대체** (더 나음) |
| 재조합 개선 | 미언급 | **새 경로는 재조합을 거치지 않음** |
| 신규 | — | 질문 발굴 경로 · 근거 체크리스트 · 템플릿 이미지 복수 |

---

# 보론 — 소스 계층 재검토 (2026-09-13 추가)

본문 §2를 쓸 때 `keyword_lab` 을 충분히 확인하지 않았습니다. **소스 9개짜리
레지스트리와 질문 수집이 이미 가동 중**입니다. 아래가 정정과 보강입니다.

## 7. 이미 있는 것

`keyword_lab/sources/base.py` 의 소스 목록입니다.

| 코드 | 이름 | 상태 |
|---|---|---|
| `naver_ads` | 네이버 검색광고 | 가동 |
| `naver_suggest` | 네이버 자동완성 | 가동 |
| `google_suggest` | 구글 자동완성 | 가동 |
| `google_planner` | 구글 키워드플래너 | 가동 |
| `google_trends` | 구글 트렌드 | 가동 |
| `gsc` | **서치콘솔 실측 쿼리** | 가동 |
| `google_trending` | **구글 실시간 인기(발견)** | 가동 |
| `naver_datalab` | 데이터랩(검증용) | 가동 |
| **`question_fanout`** | **질문 팬아웃(의문사 확장)** | **가동** |

`registry.py` 가 디스패치하고, 소스 하나가 죽어도 회차는 돕니다. 검색량 없는
후보는 검색광고로 보강합니다. **이미 잘 짜인 확장 지점입니다.**

`sources/questions.py` 는 키워드에 의문사(`왜·어떻게·언제·얼마·어디·뭐`)를
붙여 자동완성에 되묻습니다. **PAA 는 이미 검토하고 기각했습니다** — 공식 API
가 없고 스크래핑은 약관·차단 위험이라는 판단이 주석에 남아 있습니다. 제가
새로 제안할 것이 없습니다.

## 8. 질문 소스의 두 층위 — 지식인이 여전히 필요한 이유

자동완성 팬아웃과 지식인은 **같은 "질문"이지만 층위가 다릅니다.**

| | 질문 팬아웃 | 지식인·카페 |
|---|---|---|
| 정체 | 검색창에 **치는 쿼리** | 사람이 **쓴 문장** |
| 길이 | 짧음 | 김 |
| **상황** | **없음** | **있음** |
| 예 | `이사 견적 어떻게` | `송림동에서 주안동, 6평 짐, 엘베 없는 3층…` |

**이 차이가 11편을 만들었습니다.**

「이사 견적」에서 자동완성이 주는 건 `이사 견적 어떻게` / `이사 견적 얼마`
정도입니다. **여기서 "같은 건물 2층→3층", "경유 이사로 두 집 합치기",
"워시타워만 남았을 때"가 나오지 않습니다.** 조건을 붙이는 건 사람입니다.

```
주제 = 니치 × 상황
       ↑        ↑
   우리가 보유   사람이 만듦 → 지식인·카페에만 있음
```

→ **질문 팬아웃을 대체하지 않습니다. 그 아래 층을 채웁니다.**

## 9. 질문 소스 후보 — 접근성 기준

말씀하신 "사람들이 모여 궁금증을 갖는 다른 소스"를 조사했습니다.

| 소스 | 접근 | 상황 밀도 | 판단 |
|---|---|---|---|
| **지식iN** | 공식 API `kin.json` | 높음 | **채택** |
| **카페** | 공식 API `cafearticle.json` | 높음 | **채택** |
| 유튜브 댓글 | 공식 API (`commentThreads`) | 중간 | **2순위** |
| 아하(a-ha.io) | API 없음, 크롤링 | 높음 | 보류 |
| 당근 동네생활 | 앱 중심, 웹 접근 제한 | 높음 | 기각 |
| 쇼핑 상품 Q&A | API 없음 | 중간 | 기각 |
| PAA | — | — | **기각됨(기존 판단)** |

### 9-1. 카페 — 채택하되 제약을 알고 씁니다

`cafearticle.json` 은 같은 자격증명으로 바로 열립니다. 다만 셋을 감안해야
합니다.

1. **본문을 안 줍니다.** `title` + 짧은 `description` 만 옵니다. 지식인도
   같습니다. 상황을 더 뽑으려면 링크를 크롤링해야 하는데, 우리는
   `reference_crawling_service.py`(338줄)가 이미 있습니다.
2. **비공개 카페는 안 보입니다.** 정보가 알찬 카페일수록 가입제·등급제인
   경우가 많아 검색에 안 잡히거나 열어도 못 읽습니다.
3. **홍보글이 섞입니다.** 업체가 쓴 글이 질문처럼 보이는 경우가 많아
   **질문 판정 필터가 지식인보다 엄격해야 합니다.**

그래서 **지식인을 먼저, 카페를 그다음**으로 붙입니다. 같은 어댑터 구조라
추가 비용은 거의 없습니다.

### 9-2. 유튜브 댓글 — 2순위로 둡니다

`commentThreads` 는 공식 API이고 무료 할당량이 있습니다. **니치 영상 하나의
댓글에 "저는 이런 경우인데요"가 쌓입니다.** 상황 밀도가 꽤 높습니다.

다만 노이즈가 심하고(감상·인사·스팸) 질문 판정 비용이 큽니다. 지식인·카페가
자리를 잡은 뒤에 봅니다.

### 9-3. 아하·당근 — 보류·기각

**아하**는 상황 밀도가 높지만 공식 API가 없습니다. 크롤링은 약관 확인이
선행돼야 하고, 차단되면 파이프라인이 멈춥니다. PAA를 기각한 논리가 여기에도
그대로 적용됩니다.

**당근 동네생활**은 앱 중심이라 접근 자체가 어렵습니다. 지역 기반이라
이사·청소 니치와 궁합은 좋지만 **현실적 경로가 없습니다.**

## 10. 네이버 홈피드 — 다시 답합니다

본문에서 "난이도 중간, 우선순위 낮음"으로만 처리하고 넘어갔습니다. 제대로
답하면 이렇습니다.

### 10-1. 영상은 홈피드를 두 용도로 썼습니다

| 용도 | 영상에서 한 일 |
|---|---|
| ① 스타일 학습 | 홈피드 카테크 탭을 **스크롤하며 제목·본문 구조 분석** |
| ② 주제 참고 | 지금 노출되는 글의 소재 확인 |

**주목적은 ①입니다.** 주제는 나중에 뉴스 3곳에서 따로 뽑았습니다.

### 10-2. 우리에겐 이미 등가물이 있습니다

| 홈피드의 기능 | 블로그 오토의 등가물 | 상태 |
|---|---|---|
| 지금 뜨는 주제 | `SRC_GOOGLE_TRENDING` (시드 없는 발견 소스) | **가동 중** |
| 실제 노출 데이터 | `SRC_GSC` — **추정이 아닌 내 블로그 실측** | **가동 중** |
| 제목·구조 패턴 학습 | `title_gen/angles.py` (각도 추출) | **뼈대 있음** |

`base.py` 주석이 GSC를 이렇게 적어뒀습니다.

> "서치콘솔은 우리 글이 실제로 노출된 쿼리를 준다 — **추정이 아니라 실측이다.**"

**홈피드보다 강한 신호를 이미 갖고 있습니다.** 홈피드는 남의 글이 노출된
것이고, GSC는 내 글이 실제로 노출된 것입니다.

### 10-3. 그래서 결론

**네이버 홈피드를 긁을 이유는 네이버 블로그를 운영할 때뿐입니다.**

우리 발행 대상은 Blogger와 WordPress입니다. **네이버 홈피드에 노출될 일이
없는데 그 지면의 제목 스타일을 학습하면 오히려 해롭습니다.** 네이버 홈피드
제목은 구글 검색·Discover와 최적 형태가 다릅니다.

대신 **우리 지면의 역산 학습을 해야 합니다.**

- Discover 노출 → **GSC Discover 리포트** (이미 GSC 연동 있음)
- 구글 검색 상위 → `angles.py` 로 각도 추출
- CPA 전환 → 우리 실측 (클릭·전환 데이터)

→ **C-3 「노출 지면 역산 학습」의 대상을 네이버 홈피드가 아니라 GSC·구글 검색
으로 확정합니다.** 네이버 홈피드는 네이버 운영을 결정할 때 다시 꺼냅니다.

## 11. 기존 키워드/제목 수집은 어떻게 되는가

**이미 바뀌는 중이었습니다.** `docs/plans/title_pipeline_redesign_plan.md`
(2026-09-02)에 진단과 방향이 남아 있습니다.

### 11-1. 실측 진단

| 항목 | 수치 |
|---|---|
| `collected_urls` | 126,671건 (287개 도메인) |
| ㄴ 제목까지 수집 | 125,273건 (98.9%) |
| ㄴ `is_processed` | **31건 (0.02%)** |
| `temp_titles` | 105,004건 |
| `main_titles`(available) | 2,245건 → **통과율 약 2%** |

> **"수집이 부족했다"가 아니라 "수집은 과잉이고 통과율이 2%였다"**

원인 셋이 문서에 적혀 있습니다. 수집 제목을 재고로 직행시킨 것, 도메인 단위
통크롤(사이트맵 801건 통째), URL 자산이 소비되지 않은 것.

### 11-2. 전환 방향 — 재고에서 신호로

```
[옛] 경쟁 제목 수집 ──────────────→ 임시제목(재고)
[새] 경쟁 제목 조회 → 각도 추출 → AI 힌트 → 겹치지 않는 제목 생성
```

원문을 저장하지 않아 **저작권·최신성·니치 오염이 동시에 해결됩니다.**

### 11-3. 그래서 세 경로의 역할이 이렇게 갈립니다

| 경로 | 역할 | 산출 | 상태 |
|---|---|---|---|
| 도메인/제목 수집 | **각도 신호** | 프롬프트 힌트 | 전환 중 |
| 키워드 + 질문 팬아웃 | **쿼리 축** | 검색되는 말 | 가동 |
| **질문 발굴(신설)** | **상황 축** | 구체적 조건 | **추가** |

**셋은 대체 관계가 아니라 층위 관계입니다.**

- 키워드가 **무엇에 대해** 쓸지 정하고
- 질문 팬아웃이 **어떤 각도로** 검색되는지 알려주고
- 질문 발굴이 **어떤 상황의** 이야기인지 준다

수작업 이사노트 글이 정확히 이 셋을 다 썼습니다. 니치(이사 견적) × 검색되는
말(견적·비용·사다리차) × 상황(2층→3층, 엘베 없음, 장롱 3짝).

### 11-4. 도메인 통크롤은 줄입니다

`bulk_title_collector_service.py`(975줄)가 키워드당 최대 100 URL, 도메인당
사이트맵 통째를 긁습니다. **12만 건을 쌓고 31건을 썼습니다.**

질문 발굴이 자리를 잡으면 **이 경로는 각도 신호용으로만 남기고 수집량을 크게
줄이는 게 맞습니다.** 다만 이번 작업 범위 밖이라 **별도 판단 사항**으로
남깁니다.

## 12. §4 작업 단계 수정

1단계를 다음과 같이 바꿉니다.

| 항목 | 붙일 곳 | 비고 |
|---|---|---|
| ~~네이버 검색 소스 어댑터화~~ | ~~`reference_search_service.py`~~ | **위치 변경** |
| **`SRC_KIN` 소스 추가** | **`keyword_lab/sources/`** | 레지스트리에 등록 |
| **`SRC_CAFE` 소스 추가** | 〃 | 질문 판정 강화 |
| 질문 본문 크롤링 | `reference_crawling_service.py` 재사용 | 상황 추출용 |
| 참조 검색 소스 확장 | `reference_search_service.py` | **근거용은 여기가 맞음** |

**두 곳이 다릅니다.**

- `keyword_lab/sources/` → **주제를 찾는** 소스
- `reference_search_service` → **근거를 찾는** 소스

지식인·카페는 **주제 쪽**이고, 법령·뉴스는 **근거 쪽**입니다. 본문 §4에서 이
둘을 한 덩어리로 적은 것을 분리합니다.

## 13. 보론 요약

- `keyword_lab` 에 **소스 9개 레지스트리와 질문 팬아웃이 이미 있습니다**
- **PAA는 이미 기각됐습니다** (약관·차단) — 새로 제안할 것 없음
- 지식인·카페는 팬아웃의 **대체가 아니라 하위 층**. 팬아웃엔 **상황이 없습니다**
- 카페는 채택하되 **본문 미제공 · 비공개 카페 · 홍보글**을 감안
- 유튜브 댓글은 2순위, **아하는 보류**(API 없음), **당근은 기각**(접근 불가)
- **네이버 홈피드는 우리 지면이 아니라 학습 대상이 아닙니다.** 등가물인
  `google_trending` · `gsc` 가 이미 가동 중이고 **GSC가 더 강한 신호**입니다
- C-3 역산 학습 대상을 **GSC·구글 검색으로 확정**
- 기존 수집은 **이미 "재고 → 각도 신호"로 전환 중**. 질문 발굴은 그 위에
  **상황 축**을 얹는 것이지 대체가 아닙니다
- 소스 추가 위치를 **`keyword_lab`(주제) / `reference`(근거)** 로 분리

---

# 구현 완료 기록 (2026-09-13)

1~4단계를 모두 반영했습니다. **신규 테스트 141건 통과, 기존 회귀 0건**
(작업 전 커밋 `45a3356` 에서도 실패하던 10건은 그대로).

## 만든 것

| 단계 | 파일 | 줄 | 역할 |
|---|---|---|---|
| 1 | `keyword_lab/sources/base.py` | 135 | `SRC_NAVER_KIN/CAFE`, `SITUATION_SOURCES` |
| 1 | `keyword_lab/sources/community.py` | 254 | 지식iN·카페 질문 수집, 홍보글 배제 |
| 1 | `keyword_lab/sources/registry.py` | 209 | 디스패치 + 검색량 보강 제외 |
| 1 | `reference_search_service.py` | 224 | 6개 소스 + `search_many()` |
| 2 | `title_source.py` | 88 | `SRC_QUESTION` |
| 2 | `keyword_lab/situation.py` | 161 | 상황 추출·지문·중복 대조 |
| 2 | `keyword_lab/question_miner.py` | 210 | 수집→상황→제목→TitleGate |
| 3 | `reference/evidence_checklist.py` | 285 | 목록 생성·판정·재검색 정책 |
| 3 | `reference/checklist_runner.py` | 169 | 병렬 검색·진입점 |
| 3 | `generation/quality_gate.py` | 233 | AI 흔적 검사 |
| 4 | `generation/variant_picker.py` | 169 | 변형 선택 공용 |
| 4 | `generation/prompt_rotation.py` | 171 | 목적 분기 + 로테이션 |
| 4 | `generation/module_scope.py` | 111 | 모듈 담당 범위 라우팅 |
| 4 | `generation/template_image_service.py` | 474 | 템플릿 복수화 |
| 연결 | `generation/reference_collector.py` | 486 | 체크리스트 물림 |
| 연결 | `generation/flow_generate_executor.py` | 473 | 로테이션 물림 |

전부 500줄 미만입니다.

## 켜는 법

**체크리스트** — 참조 설정에 넣습니다. 기본은 꺼짐.

```json
"evidence_checklist": {"enabled": true, "ai_provider": "openai"}
```

**프롬프트 로테이션** — 모듈 설정.

```json
"prompt_rotation": {
  "enabled": true, "mode": "sequential",
  "variants": [
    {"code": "howto-guide", "purpose": "adsense", "topic_ids": [24]},
    {"template": "직접 쓴 프롬프트", "purpose": "cpa"}
  ]
}
```

**템플릿 이미지 복수** — 블로그 `overlay_config`.

```json
"template_images": [{"path": "blogs/21/t1.png", "topic_ids": [24]},
                    {"path": "blogs/21/t2.png"}],
"template_image_mode": "random"
```

**커뮤니티 소스** — 키워드 모듈 설정의 `sources` 에 `naver_kin`,
`naver_cafe` 를 넣습니다.

**AI 흔적 차단** — 경고가 기본. 차단하려면
`"quality_gate": {"trace_blocks": true}`.

## 남은 것

- **니치별 모듈 분리 UI** — 백엔드 라우팅(`module_scope`)은 끝났습니다.
  블로그 선택 시 니치가 전부 잡히는 화면 동작을 부분 선택으로 바꿔야
  전담 모듈을 만들 수 있습니다.
- **질문 발굴 실행 경로** — `QuestionMiner` 는 완성됐지만 오토런·라우터에
  물리지 않았습니다. 지문 보관 위치(모듈 설정 vs 실행 상태)를 정해야 합니다.
- **모델 배분 실측** — 체크리스트 생성·판정은 판단 작업이라 가성비 모델이
  제대로 못 할 수 있습니다. 켜기 전에 같은 제목으로 A/B 가 필요합니다.
- **도메인 통크롤 감축** — 12만 건 쌓고 31건 사용. 별도 판단 사항.
